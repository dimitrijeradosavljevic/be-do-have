import { ActionReducerMap } from '@ngrx/store';

// import { AuthState, authReducer } from '../../auth/store/auth.reducer';
// import { PageState, reducer as pageReducer } from '../../page/store/page.reducer';
// import { OrganisationState, organisationReducer } from '../../organisation/store/organisation.reducer';

export interface State {
  // auth:         AuthState;
  // page:         PageState;
  // organisation: OrganisationState;
}

export const reducers: ActionReducerMap<State> = {
  // auth:         authReducer,
  // page:         pageReducer,
  // organisation: organisationReducer
};

